# Core ML + data stack for the detection model
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Plotting (used in the notebooks for exploration / evaluation)
matplotlib>=3.7
seaborn>=0.12

# Notebooks
jupyter>=1.0

# Testing
pytest>=7.4

# Local LLM report backend (default as of 2026-07-19, see src/config.py
# REPORT_BACKEND). Ollama itself is a separate desktop install
# (https://ollama.com) plus `ollama pull llama3.1:8b`; this is just the Python
# client. If it's missing, or the Ollama app isn't running, report generation
# automatically falls back to the template backend, which works without this.
ollama>=0.3
